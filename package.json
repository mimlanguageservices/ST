{
  "name": "essay-submission-system",
  "version": "1.0.0",
  "description": "Essay submission system with Claude API correction and email functionality",
  "main": "index.html",
  "scripts": {
    "build": "echo 'No build step required'",
    "dev": "netlify dev"
  },
  "dependencies": {
    "@sendgrid/mail": "^7.7.0",
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}